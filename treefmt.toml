[formatter.nix]
command = "nixpkgs-fmt"
options = []
includes = ["*.nix"]
excludes = []

[formatter.dprint]
command = "dprint"
options = ["fmt"]
includes = ["*.json", "*.md", "*.toml"]
excludes = ["profile-summary-card-output/**"]

[formatter.yaml]
command = "yamlfmt"
options = []
includes = ["*.yaml", "*.yml"]
excludes = []
